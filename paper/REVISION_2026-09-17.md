# OneStroke2026：可投版本修订说明（2026-09-17）

本轮基于 2026-09-05 修订包（SHA-256
`CAAF652A66489CE6E6C0024D72F69941D16CD9EA5EC5762F2FFE986E0E5B3B7E`），
保留题目、C1–C4 / RQ1–RQ4、方法流程、全部实验数据、模型排序与原有引用。
修改涉及引言开篇、摘要首句、Fig. 1 的 (a) 面板、正文 Fig. 3 的解释文字，
以及版面上的浮动体间距。输出快照见 `releases/2026-09-17-submission/`。

## 0. 摘要首句（概括引言开头的新内容）

摘要 Purpose 首句补充书法本体与书体的概括，与引言开篇呼应：

> Calligraphy, the brush-written Chinese art whose seal, clerical, regular,
> running, and cursive styles differ in stroke technique, is a foundation of
> traditional culture, yet two legible instances of the same character can
> still differ in the local structure that instruction attends to.

为不越过 IJDAR 的 250 词上限，同一段内做了等量压缩：删除
"similarity"、"The frozen"、"respectively" 等冗余词，把
"points"、"Three structural scores"、"constrained similarity registration"
改为更短的表述。压缩后摘要 228 词（原 213 词），仍满足 150–250 词要求，
且未增删任何数值或结论。

## 0b. Fig. 1 面板 (a)：由一对字扩展为三对字

原 Fig. 1 的 (a) 只对比一个汉字。新版 (a) 展示外部参照库中 7 对同字跨体对的
前 3 对，行=目标汉字，列=风格角色：

```text
              reference   candidate
  亮（第 1 行）  欧阳询楷书    王羲之行书   <- 原冻结样本，像素完全复用
  息（第 2 行）  欧阳询楷书    王羲之行书
  尉（第 3 行）  欧阳询楷书    王羲之行书
```

选样规则固定在脚本里：取冻结的流水线样本对（汉字 亮），再按冻结的
`cross_reference_pairs.csv` 顺序取其后两对；三对都已在正文 Fig. 3(c) 中出现，
不是按外观挑选。

实现方式（`figures/revision/build_figure1.py`）：把冻结图整页作为矢量内容放置，
用 PDF redaction 真正删除旧 (a) 面板的图与文字，再绘制新画廊与标签。
因此 (b)–(e) 及底部图例与已发表版本完全一致；脚本自检在 600 dpi 下
(b)–(e) 区域与冻结图**逐像素零差异**，全页差异仅落在 x 4.6–67.7 pt、
y 4.9–117.8 pt 的 (a) 面板与标题范围内。第 1 行复用原冻结的两张内嵌图，
第 2、3 行取参照缓存对应的 256×256 原图，按原有的 194×194 规则缩放。
标签沿用冻结用词、字号与颜色，字体从冻结 PDF 中提取（LMSansTT8/10），
汉字标签用微软雅黑，全部为矢量可选中文本。页码与版面高度不变，
主稿仍为 20 页。

### (a) 面板改为与右侧统一的卡片板式

原 (a) 面板的输入字是**无边框的纯白照片**，而右侧 b–e 是带灰色细边框、
落在图内灰板上的卡片，底部图例条在 "Input pair" 处还留了一个白色圆角格，
因此左栏看起来与右侧不统一。(a) 现在改用同一套板式：

```text
贴图纸底     白            ->  247, 248, 250   （PANEL_BG，与面板 (e) 卡片同色）
贴图外框     无            ->  0.55 pt 细线, 215, 220, 225（GRID，与 b–d 窗框同宽）
底部图例条   左侧白格      ->  贯通 238, 240, 242 灰条
```

纸底映射只用一条逐通道线性函数 `out = round(C * plate / 255)`：白纸正好落到
板灰，黑色笔画保持为 0，不做对比度、伽马、阈值、裁剪或重采样。由于映射线性
且墨色锚定在零，**相对纸底的笔画浓度** `(paper - min(RGB)) / paper` 保持不变；
构建脚本会逐块断言并把最大偏差写入 JSON（实测 2.1e-3，即不超过一个 8 bit 色阶）。
第 1 行的墨迹仍来自原冻结贴图，只是纸底换成了板灰。

另外，底部图例条上 "Input pair → Overlapping parse" 之间的小箭头原本落在
被 redaction 清除的范围内，一并被删掉了。现在把该窄条（x 60.4–68.6 pt、
y 107.5–121.18 pt）从冻结矢量图原样重新插入，四枚箭头的 600 dpi 渲染与冻结图
**逐像素零差异**（暗像素计数分别为 933 / 946 / 943 / 938），脚本会断言这一点。

## 1. 引言开篇

按要求在 `sections/01_introduction.tex` 第一段最前面加入书法本体与文化定位：
书法是以毛笔书写汉字的中国传统文化形态；篆、隶、楷、行、草各体笔法不同，
可表达端庄、秀逸、雄浑、奔放、古拙、灵动等风格与情感；同一个汉字的两个手写
实例即使都能被清晰识别，在书法教学关注的局部结构上仍可能存在差异。

原稿已有的 “Two handwritten instances … equally legible yet differ in the
local structure …” 一句保留在其后，避免重复论述或削弱原有表述；后面承接
教师指出笔画偏短、钩画移位等具体例子，段落衔接不变。这段是把中文原意译成
学术英文，没有新增任何数据性或结论性主张。

## 2. 正文 Fig. 3 的文字解释

正文 Fig. 3（`figures/revision/figure3_dataset_overview`）此前只有图注说明，
图内缺少自解释文字。本轮改动分两处：

- 图内：面板 (a) 增加 “Semantic audit; exclusions frozen before any
  evaluation”；面板 (b) 说明该面板是训练与评测语料（QC-clean 每字符一个
  代表样本）；面板 (c) 明确这是**独立的外部参照库**，其掩膜为解析器输出。
- 正文：`sections/03_data_resources.tex` 在 Fig. 3 之后增加一段，逐面板说明
  读者应当看到什么，并再次强调 (c) 的缓存掩膜不是分割标注真值。

图内没有改动任何样本像素、面板内容或计数。重跑 `build_figure3.py` 后脚本
自检全部 54 张嵌入图像的原生像素哈希与源文件一致，并确认新文字与既有文字
零重叠；894 − 54 = 840、840 − 12 − 59 = 769 的核对保持不变。

## 3. 版面与页数

加入上述两段文字后主稿一度变成 21 页。本轮没有删除任何内容，而是收紧浮动体
与图注间距，使新增文字在 20 页限制内被吸收：

```tex
\setlength{\textfloatsep}{9pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{9pt plus 2pt minus 2pt}
\setlength{\floatsep}{11pt plus 2pt minus 2pt}
\setlength{\dblfloatsep}{11pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{2pt}
\setlength{\belowcaptionskip}{4pt}
```

逐页检查后，正文列底未出现新的明显空白带；已知的两处列底空隙（第 7 页右栏
约 43 pt、第 12 页左栏约 60 pt）来自双栏浮动体与标题保持规则，属于 LaTeX
常规排版行为，未用灌水文字填补。

## 4. 构建校验

- `pdflatex` → `bibtex` → `pdflatex` ×2：主稿 20 页，ESM 4 页。
- `manuscript.log` 与 `ESM_1.log` 中 `Overfull` 与 `undefined` 计数均为 0。
- 末页正文结束于 842 pt 页面的 672 pt，页面填满，无结尾空白页。
- 在线编辑源码包重新生成：102 个文件，可导入 Overleaf。

## 5. 投稿前仍需作者处理

以下不是构建问题，而是需要作者或单位确认的事项：

- 伦理审批 / 知情同意表述与数据可得性声明的最终确认；
- Calli-Tongji（CC BY-NC 4.0）参考图在商业出版社文章中的转载许可确认，
  若不允许需替换为可再分发素材；
- 作者简介文本与证件照由投稿系统单独提交。
